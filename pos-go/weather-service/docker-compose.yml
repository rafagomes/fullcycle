version: "3.8"

services:
  weather-service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - WEATHER_API_KEY=36a78590564d420288411225250305
    networks:
      - weather-network

networks:
  weather-network:
    driver: bridge
